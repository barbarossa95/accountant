import React from 'react';

import OperationList from '../components/OperationList';

const TimeTable = ({ operations }) => {
  return <OperationList operations={operations} />;
};

export default TimeTable;
